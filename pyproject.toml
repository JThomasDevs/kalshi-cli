[project]
name = "kalshi-cli"
version = "1.2.3"
description = "Interactive CLI for trading prediction markets on Kalshi"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "cryptography>=41.0.0",
    "rich>=13.7.0",
    "typer>=0.9.0"
]

[project.scripts]
kalshi = "cli:app"
